# 贡献指南

欢迎修复问题、改进设备兼容性和文档。请先阅读 [README](README.md) 与 [开发说明](docs/development.md)。

## 范围

项目只负责媒体命令到可配置快捷键的映射。新增音频采集、目标应用专用逻辑、录音状态推断、多种转发链路或云端服务前，请先通过 issue 讨论；避免重新引入复杂的应用控制层。

## 提交改动

1. 从 `main` 创建工作分支，保持改动集中。
2. 对影响行为的改动补充有意义的回归测试。
3. 运行 `./scripts/test.sh` 和 `./scripts/build.sh`。
4. 同步更新受影响的配置和文档。
5. 在 PR 中说明问题、改变后的行为、验证方法及尚未验证的硬件边界。

不要提交应用包、日志、录音、个人配置、签名凭据或绝对个人路径。普通测试应不依赖辅助功能授权、不向当前应用发送真实按键。新增依赖必须说明用途与许可。

## 问题报告

请提供系统版本、芯片架构、设备型号、EarTap 版本、预期与实际行为，以及计数是否变化。对比手动快捷键或菜单测试可帮助判断故障层，但不要求为了报告问题进行反复按节奏测试。

提交内容须有权按项目的 MIT 许可证分发。
