# 更新记录

## 0.1.5 — 2026-09-21

- 状态栏图标改为直接绘制白色像素并关闭模板模式，避免 macOS 将图标重新渲染为黑色。
- 移除状态栏按钮的模板 tint 依赖，保留各状态的形状和暂停透明度差异。

## 0.1.4 — 2026-09-21

- 应用图标改为深蓝紫背景上的白色耳机与声波，以白色作为主体视觉。
- 状态栏图标统一使用白色，仅通过就绪、暂停、发送中和错误的图形变化及暂停透明度区分状态。

## 0.1.3 — 2026-09-21

- 新增 EarTap 应用图标，并在构建时生成完整尺寸的 macOS `.icns` 资源。
- 菜单栏改用耳机模板图标，以蓝色、灰色、绿色和红色区分就绪、暂停、发送中和错误状态。
- 状态图标包含辅助功能标签，并补充状态选择和图标属性回归测试。

## 0.1.2 — 2026-09-21

- 恢复 `pause` 媒体事件的快捷键映射，修复部分耳机点击上报 `pause` 时没有反应的回归。
- `play` / `pause` / `toggle` 重新使用同一套状态检查、排队和发送链路，并补充三种媒体回调的集成回归测试。
- 暂不区分正常点击与耳机关机产生的 `pause`；关机误触将在后续版本另行处理。

## 0.1.1 — 2026-09-20

- 过滤 `pause` 媒体事件，记录明确忽略日志，不入队或发送快捷键，避免已观察到的耳机关机误触。
- 保持 `play` / `toggle` 和现有快捷键配置不变；增加调度与媒体回调集成回归测试。
- 不涉及录音中第二次按键未送达或蓝牙连接状态；修复后的实体效果待验证。

## 0.1.0 — 2026-09-20

- 发布独立的 EarTap 菜单栏应用。
- 播放、暂停和切换媒体命令统一映射为自定义快捷键。
- 支持配置热更新、暂停、日志、权限检查和延迟手动测试。
- 提供构建、安装、自动测试与 macOS CI。
- 接收能力受系统媒体路由影响，尚未建立完整设备兼容性矩阵。
